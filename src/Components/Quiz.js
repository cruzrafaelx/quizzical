import React from "react";

export default function Quiz({quiz}){
      
      return(
            <section className="questions-container" >
              {
                  // quiz.map(item => {
            
                  // })
              }
                 
            </section >
      )
}